@model IEnumerable<property_lease_saas.Models.Entities.LeaseRequest>

<table class="table table-hover">
<thead>
<tr>
    <th>Property</th>
    <th>Requested On</th>
    <th>Status</th>
    <th>Action</th>
</tr>
</thead>

<tbody>
@foreach (LeaseRequest r in Model)
{
    <tr>
        <td>@r.Property.Title</td>
        <td>@r.Property</td>
        <td>@r.RequestedAt</td>
        <td>@r.TenantId</td>

        <td>
            <span class="badge 
                @(r.Status == LeaseRequestStatus.Pending ? "bg-warning" :
                  r.Status == LeaseRequestStatus.Approved ? "bg-success" : "bg-danger")">
                @r.Status
            </span>
        </td>

        <td>
            @if (r.Status == LeaseRequestStatus.Pending)
            {
                <form asp-controller="Lease" asp-action="Approve" method="post" class="d-inline">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="leaseRequestId" value="@r.Id" />
                    <button type="submit" class="btn btn-sm btn-success">
                        Approve
                    </button>
                </form>

                <form asp-controller="Lease" asp-action="Reject" method="post" class="d-inline">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="leaseRequestId" value="@r.Id" />
                    <button type="submit" class="btn btn-sm btn-danger">
                        Reject
                    </button>
                </form>
            }
        </td>
    </tr>
}

</tbody>
</table>
